version: "3.8"

services:
  hmlv-kanban:
    build: .
    container_name: hmlv-kanban
    restart: unless-stopped
    ports:
      - "8081:5678"
    networks:
      - public-net
    env_file:
      - .env
    volumes:
      - ./wiptrack_server.py:/app/wiptrack_server.py:ro
      - ./HMLV生产看板.html:/app/HMLV生产看板.html:ro
      - /etc/localtime:/etc/localtime:ro

networks:
  public-net:
    external: true
