# 2026-05-13 执行记录

## 测试工序数据源替换（LineWO表）

**需求**：将HMLV生产看板工序WIP测试完成工单数据从原production_records表替换为SUZVSQL01数据库Smith库LineWO表，并筛选掉P2产线数据。

**执行内容**：
1. 连接SUZVSQL01.Smith.dbo.LineWO表，确认表结构：WO, PN, Start_Date, End_Date, Productivity, Tester, WOnumber, Line, LineCount, Offset, Date
2. 查看产线数据：LINE 01-26, Assembly, Kit-1~5, TX等，P2产线（P2-1~P2-11等）共5093条记录
3. 修改wiptrack_server.py：
   - 添加SUZVSQL01数据库配置（SERVER=SUZVSQL01, DATABASE=Smith, UID=rptuser1, PWD=CHNX#123）
   - 新增`get_test_data_from_suzvsql01()`函数，从LineWO表获取测试数据，WHERE条件排除P2/p2产线，使用窗口函数ROW_NUMBER()去重（每个工单只保留最新记录），只获取当月数据
   - 修改`get_data()`函数，合并原有production_records数据和新的LineWO测试数据
   - 修改`/api/wip`接口，合并LineWO测试数据到job_station_time字典，确保WIP计算包含测试工序数据
   - **关键修改**：在查询production_records表时排除Test工序数据（`AND Station NOT LIKE '%Test%'`），确保测试工序数据**只来自LineWO表**
   - 修复日期范围变量：将硬编码的5月范围改为动态计算当月范围（month_start/month_end）
4. 重启Flask服务器（端口5678），测试API正常返回
5. 验证结果：
   - `/api/data`：测试工序WIP数量74
   - `/api/wip`：测试工序完成**73个**，滞留**127个**；包装工序完成105个，滞留33个
   - 数据源切换成功，测试工序数据**只从LineWO表获取**

## 组装工序数据源替换（LineWO_Assembly表）

**需求**：将HMLV生产看板工序WIP组装完成工单数据从原production_records表替换为SUZVSQL01数据库Smith库LineWO_Assembly表。

**执行内容**：
1. 连接SUZVSQL01.Smith.dbo.LineWO_Assembly表，确认表结构与LineWO相同：WO, PN, Start_Date, End_Date, Productivity, Tester, WOnumber, Line, LineCount, Offset, Date
2. 查看产线数据：LINE 01-12, LINE 15-19, LINE 23-25, ASSEMBLY-1, KIT-1, KIT-4，共27263条记录，无P2产线
3. 修改wiptrack_server.py：
   - 新增`get_asm_data_from_suzvsql01()`函数，从LineWO_Assembly表获取组装数据，使用窗口函数ROW_NUMBER()去重（每个工单只保留最新记录），只获取当月数据
   - 修改`/api/wip`接口，合并LineWO_Assembly组装数据到job_station_time字典
   - 修改production_records查询，排除Assembly工序数据（`AND Station NOT LIKE '%Asm%' AND Station NOT LIKE '%Assembly%'`），确保组装工序数据**只来自LineWO_Assembly表**
4. 重启Flask服务器（端口5678），测试API正常返回
5. 验证结果：
   - `/api/wip`：测试工序完成**73个**，滞留**127个**；组装工序完成**180个**，滞留**36个**
   - 数据源切换成功，组装工序数据**只从LineWO_Assembly表获取**

**数据验证**：
- LineWO表5月完成测试（排除P2）: 73个工单（去重后）
- LineWO_Assembly表5月完成组装: 180个工单（去重后），223条记录（不去重）
- production_records表5月完成测试: 118个工单（已排除）
- production_records表5月完成组装: 已排除
- **最终结果**: 测试73个 + 组装180个（只来自LineWO和LineWO_Assembly表，已去重）

**文件位置**：`C:\Users\guangli.wang\WorkBuddy\20260424162556\wiptrack_server.py`

**注意事项**：
- LineWO表使用End_Date作为完成时间
- LineWO_Assembly表使用End_Date作为完成时间
- 筛选条件：`Line NOT LIKE '%P2%' AND Line NOT LIKE '%p2%' AND End_Date IS NOT NULL AND End_Date >= 当月第一天 AND End_Date <= 当月最后一天`
- 数据格式统一：工单号(WO)→Job字段，Station='Test'/'Asm'，CompleteDate=End_Date
- 原计划使用LineWO_WLB表，后改为LineWO表（数据量更大，131878条 vs 3999条）
- WIP计算逻辑：根据Line和完成时间计算测试/组装已完成数据，然后流转至下一道工序
- 使用窗口函数ROW_NUMBER() OVER (PARTITION BY WO ORDER BY End_Date DESC)去重，每个工单只保留最新记录
- 日期范围动态计算：使用calendar.monthrange获取当月最后一天
- **测试工序数据只从LineWO表获取，组装工序数据只从LineWO_Assembly表获取，production_records表中的Test/Assembly工序数据已排除**
